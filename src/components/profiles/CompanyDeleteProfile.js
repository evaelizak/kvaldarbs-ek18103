import { Button } from 'antd';
import React from 'react';

const CompanyDeleteProfile = () => {
  return (
    <Button danger type="primary" className="mt-5">
      Delete company TBA
    </Button>
  );
};

export default CompanyDeleteProfile;
